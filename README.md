# SkyRoute-Flight-Planner
A C++ flight planning application using graph-based search algorithms (BFS and UCS) to find optimal travel routes. Built with the Bobcat UI framework for FLTK, the interface supports dynamic itinerary generation, CSV-driven data handling, and efficient memory management.
